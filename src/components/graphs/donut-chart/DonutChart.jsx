import React from 'react';

function DonutChart() {
// Donut chart needs to have data that is in the shape of an object.
    return (
        <section>
            hi
        </section>
    )
}

export default DonutChart;