describe("dashboard graphs", () => {
  before(() => {
    cy.visit('./');
  });

  context("default settings", () => {
    it('The title of the graph should match the text of the button', () => {
      // cy.get('')
    });
  })
});